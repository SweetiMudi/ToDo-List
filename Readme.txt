ToDo list project
we can add items in to do list by clicking the + button while clicking the button colour will change to green.
And we can also delete the items by clicking X button it turns red and then the item will deleted successfully.
using css - the project seems quite genuine.
using react , react components and hooks feactures , array destructuring , array map
